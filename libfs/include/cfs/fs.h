/**
 * CFS Server API - Public Header
 *
 * This is the public server API for the CFS (Cache Filesystem) library.
 *
 * This header provides server-side filesystem interface for CFS.
 *
 * Usage:
 *   #include <cfs/fs.h>
 *
 * This API is guaranteed to be stable. Internal headers (server.h, session.h, namespace.h, etc.)
 * should not be used by external applications.
 *
 * @see src/cfs/server/fs.h for implementation
 */

#ifndef CFS_PUBLIC_FS_H
#define CFS_PUBLIC_FS_H

/* Include the actual implementation from the source tree */
#include "../../src/cfs/server/fs.h"

#endif /* CFS_PUBLIC_FS_H */
