/**
 * KVFS Server API - Public Header
 *
 * This is the public server API for the KVFS (Key-Value Filesystem) library.
 *
 * This header provides server-side filesystem interface for KVFS.
 *
 * Usage:
 *   #include <kvfs/fs.h>
 *
 * This API is guaranteed to be stable. Internal headers (server.h, session.h, subtable.h, etc.)
 * should not be used by external applications.
 *
 * @see src/kvfs/server/fs.h for implementation
 */

#ifndef KVFS_PUBLIC_FS_H
#define KVFS_PUBLIC_FS_H

/* Include the actual implementation from the source tree */
#include "../../src/kvfs/server/fs.h"

#endif /* KVFS_PUBLIC_FS_H */
